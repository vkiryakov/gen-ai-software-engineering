# Screenshots needed here

I can't capture your screen/terminal myself, so these still need to be taken
manually and dropped into this folder (referenced from the PR description
per the root README's submission requirements):

- [ ] `pipeline-run.png` — terminal output of `npm run pipeline` from start
      (`### Bug pipeline for 001 ###`) to finish
      (`### Pipeline complete for bug 001 ###`).
- [ ] `fixes.png` — `context/bugs/001/fix-summary.md` open (or a diff view of
      `src/routes/notes.js` showing the 7 applied changes).
- [ ] `security-scan.png` — `context/bugs/001/security-report.md` open,
      showing the findings table and severity counts.
- [ ] `unit-tests.png` — terminal output of `npm test` after the pipeline run
      (3 suites / 23 tests passing).

Delete this file once the four screenshots above are in place.
